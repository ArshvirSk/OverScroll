package com.overscroll.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Overscroll Light/Pastel palette (Redesign) ───────────────────────────────
// Warm peach to lavender gradient with a confident Teal accent.

// Primary Accent: Confident Teal
val OverscrollPrimary = Color(0xFF00BFA5) // Vibrant Teal
val OverscrollOnPrimary = Color.White
val OverscrollPrimaryContainer = Color(0xFFB2DFDB)
val OverscrollOnPrimaryContainer = Color(0xFF004D40)

// Secondary: Soft warm coral (for highlights)
val OverscrollSecondary = Color(0xFFFF8A65)
val OverscrollOnSecondary = Color.White

// Tertiary: Soft Lavender
val OverscrollTertiary = Color(0xFFB39DDB)
val OverscrollOnTertiary = Color.White

// Surface hierarchy (Bright & Airy)
val OverscrollSurface = Color(0xFFFFFFFF)
val OverscrollSurfaceVariant = Color(0xFFF5F5F7) // Very soft gray for cards
val OverscrollSurfaceElevated = Color(0xFFFFFFFF)
val OverscrollSurfaceBright = Color(0xFFFAFAFA)

// Text on bright surfaces
val OverscrollOnSurface = Color(0xFF2D3748) // Soft dark text
val OverscrollOnSurfaceVariant = Color(0xFF718096) // Muted text

// Premium Gradient Backgrounds
val OverscrollBackgroundTop = Color(0xFFFFF0EC) // Warm Peach
val OverscrollBackgroundBottom = Color(0xFFE8E4F8) // Soft Lavender

// Semantic colors
val OverscrollSuccess = Color(0xFF4ADE80)
val OverscrollWarning = Color(0xFFFBBF24)
val OverscrollError = Color(0xFFFF6B6B)

// Outline / dividers
val OverscrollOutline = Color(0xFFE2E8F0)
val OverscrollOutlineVariant = Color(0xFFEDF2F7)

// ── Chart-specific colors ─────────────────────────────────────────────────────
val ChartBarDefault = Color(0xFFE2E8F0) // Soft gray for standard bars
val ChartBarToday = OverscrollPrimary // Teal for today
val ChartGridLine = Color(0xFFEDF2F7)
